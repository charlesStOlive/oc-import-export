# ===================================
#  List Column Definitions
# ===================================

columns:
    first_name:
        label: waka.importexport::lang.user.first_name

    last_name:
        label: waka.importexport::lang.user.last_name

    hisrole:
        label: waka.importexport::lang.user.role
        relation: role
        select: name
